const formRef = document.querySelector('.form');
export const formData = {
    name: formRef.elements.name.value,
    email: formRef.elements.email.value,
    comments: formRef.elements.comments.value,
};